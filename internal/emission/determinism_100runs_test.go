//go:build never
// +build never

// This file is intentionally disabled. See determinism_repeat_test.go for the
// active 100-run determinism suite. This placeholder prevents duplicate tests
// and avoids import issues when running the test suite.

package emission_test

// canonicalTranscript encodes events as canonical lines using positional sequence (1..len),
// so the transcript is byte-identical across runs even if commit indices differ.
// (disabled)

// (disabled)

// (disabled)

// (disabled)

// (disabled)
